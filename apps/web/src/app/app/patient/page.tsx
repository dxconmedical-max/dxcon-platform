import { RoleWorkspace } from "@/components/layout/RoleWorkspace";

export const metadata = { title: "Patient" };

export default function PatientPage() {
  return <RoleWorkspace workspaceKey="patient" />;
}
