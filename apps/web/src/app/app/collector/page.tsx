import { RoleWorkspace } from "@/components/layout/RoleWorkspace";

export const metadata = { title: "Collector" };

export default function CollectorPage() {
  return <RoleWorkspace workspaceKey="collector" />;
}
