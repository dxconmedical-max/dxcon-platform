import { RoleWorkspace } from "@/components/layout/RoleWorkspace";

export const metadata = { title: "Administration" };

export default function AdminPage() {
  return <RoleWorkspace workspaceKey="admin" />;
}
