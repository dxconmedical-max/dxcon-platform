import { RoleWorkspace } from "@/components/layout/RoleWorkspace";

export const metadata = { title: "Reception" };

export default function ReceptionPage() {
  return <RoleWorkspace workspaceKey="reception" />;
}
