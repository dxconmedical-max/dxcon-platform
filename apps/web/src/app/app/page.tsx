import { AppWorkspaceRedirect } from "@/components/layout/AppWorkspaceRedirect";

export const metadata = { title: "Workspace" };

export default function AppWorkspacePage() {
  return <AppWorkspaceRedirect />;
}
