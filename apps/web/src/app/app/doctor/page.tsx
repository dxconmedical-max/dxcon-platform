import { RoleWorkspace } from "@/components/layout/RoleWorkspace";

export const metadata = { title: "Doctor" };

export default function DoctorPage() {
  return <RoleWorkspace workspaceKey="doctor" />;
}
