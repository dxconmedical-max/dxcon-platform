"use client";

/** Architecture placeholder — no payment UI logic. */
export function PaymentPanelPlaceholder() {
  return (
    <div className="rounded-md border border-dashed border-neutral-300 p-4 text-sm text-neutral-600">
      PaymentPanel — foundation placeholder. Collect payment UI not implemented in M2 foundation.
    </div>
  );
}
