# ADR-003 API-First Platform

**Status:** Accepted (Frozen Release 2.0)

## Decision

All capabilities exposed via versioned REST API (`/api/v1/`). Web UIs consume APIs.

## Consequences

- OpenAPI inventory maintained.
- Breaking changes require v2.
