# Test Code Mapping

Mappings live in `integration_test_mappings` with:

- `analyzer_test_code` ↔ `dxcon_test_code`
- unit, specimen type, LOINC foundation
- version, approver, effective date

Changes require authorized lab manager approval. Unmapped results are quarantined.
